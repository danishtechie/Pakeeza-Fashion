node_modules
.next
.env
.env.local
*.db
*.db-journal
/public/uploads/*
!/public/uploads/.gitkeep
/public/seed/*
!/public/seed/.gitkeep
# Seeded demo images are committed so the deployed catalog can display them.
!/public/seed/*.png
dist
